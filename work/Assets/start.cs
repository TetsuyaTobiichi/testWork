using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class startClickButton
{
    public GameObject mainMenu;
    // Start is called before the first frame update
    void startClick()
    {
        mainMenu.SetActive(false);
    }
}
